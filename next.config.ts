import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: [
      "i3.wp.com",
      "i2.wp.com",
      "i0.wp.com",
      "i1.wp.com",
      "kuronime.biz",
      "cdn.jsdelivr.net",
      "www.mp4upload.com",
    ],
  },
};

export default nextConfig;
